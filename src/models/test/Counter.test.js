describe("Counter", () => {
  test("should insert 1001 in sequence value", () => {
    expect(() => {}).toBeTruthy();
  });

  test("should increment sequence value by 1", () => {
    expect(() => {}).toThrow();
  });
});
