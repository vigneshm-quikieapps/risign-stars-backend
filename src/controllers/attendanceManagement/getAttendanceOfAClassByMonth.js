const {
  AttendanceOfAClassByMonth,
} = require("../../models/attendanceManagement");

/**
 * get all the attendance of a class by month
 * @param {*} req
 * @param {*} res
 * @returns
 */
const getAttendanceOfAClassByMonth = async (req, res) => {
  /**
   * TODO
   */
};

module.exports = getAttendanceOfAClassByMonth;
