const {
  AttendanceOfAClassByMonth,
} = require("../../models/attendanceManagement");

/**
 * get attendance of a member in a class by month
 * @param {*} req
 * @param {*} res
 * @returns
 */
const getAttendanceOfAMemberInAClassByMonth = async (req, res) => {
  /**
   * TODO
   */
};

module.exports = getAttendanceOfAMemberInAClassByMonth;
