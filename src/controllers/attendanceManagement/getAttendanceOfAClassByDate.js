const {
  AttendanceOfAClassByDate,
} = require("../../models/attendanceManagement");

/**
 * get all the attendance of a class by date
 * @param {*} req
 * @param {*} res
 * @returns
 */
const getAttendanceOfAClassByDate = async (req, res) => {
  /**
   * TODO
   */
};

module.exports = getAttendanceOfAClassByDate;
