const partialCharge = () => {
  /** get no. of days enrolled for the month */
  let startDate = new Date();

  let charge = "";
  let noOfSessions = "";
  return (charge / 4) * noOfSessions;
};

module.exports = partialCharge;
